diff --git a/pkg/agent/agent_message.go b/pkg/agent/agent_message.go
index 419664ed..335dd0bd 100644
--- a/pkg/agent/agent_message.go
+++ b/pkg/agent/agent_message.go
@@ -205,6 +205,25 @@ func (al *AgentLoop) processMessage(ctx context.Context, msg bus.InboundMessage)
 		return response, nil
 	}
 
+	if handled, err := al.handleDirectVineyardReportRoute(ctx, msg, opts.Dispatch.SessionKey); handled {
+		return "", err
+	}
+
+	if skill, prompt, ok := forcedVineyardSkillRoute(msg.Channel, msg.Content); ok {
+		opts.NoHistory = true
+		if skill != "" {
+			opts.ForcedSkills = appendMissingSkill(opts.ForcedSkills, skill)
+		}
+		opts.Dispatch.UserMessage = prompt
+		opts.UserMessage = opts.Dispatch.UserMessage
+		logger.InfoCF("agent", "Forced vineyard skill-first Telegram route",
+			map[string]any{
+				"session_key": opts.Dispatch.SessionKey,
+				"skill":       skill,
+				"channel":     msg.Channel,
+			})
+	}
+
 	if pending := al.takePendingSkills(opts.Dispatch.SessionKey); len(pending) > 0 {
 		opts.ForcedSkills = append(opts.ForcedSkills, pending...)
 		logger.InfoCF("agent", "Applying pending skill override",
diff --git a/pkg/agent/agent_outbound.go b/pkg/agent/agent_outbound.go
index f5de6fa4..22848c3f 100644
--- a/pkg/agent/agent_outbound.go
+++ b/pkg/agent/agent_outbound.go
@@ -74,6 +74,13 @@ func (al *AgentLoop) PublishResponseIfNeeded(ctx context.Context, channel, chatI
 		return
 	}
 
+	if al.publishStructuredResponseIfNeeded(ctx, channel, chatID, sessionKey, response) {
+		return
+	}
+	if looksLikeStaleVineyardSummary(response) {
+		response = "Blocked stale vineyard answer: session memory is not a truth source. Call daily-vineyard-briefing and send its structured send_text plus photo attachments."
+	}
+
 	msg := bus.OutboundMessage{
 		Context:    bus.NewOutboundContext(channel, chatID, ""),
 		SessionKey: sessionKey,
diff --git a/pkg/agent/context.go b/pkg/agent/context.go
index 74c278f3..2e18a953 100644
--- a/pkg/agent/context.go
+++ b/pkg/agent/context.go
@@ -174,9 +174,9 @@ func (cb *ContextBuilder) getIdentity(includeToolUseRule bool) string {
 	}
 
 	return fmt.Sprintf(
-		`# picoclaw 🦞 (%s)
+		`# PicoClaw 🍇 (%s)
 
-You are picoclaw, a helpful AI assistant.
+You are PicoClaw 🍇, the board orchestrator. Load local contracts and skills before answering board, hardware, weather, vineyard, or Goidanich questions.
 
 ## Workspace
 Your workspace is at: %s
diff --git a/pkg/agent/pipeline_execute.go b/pkg/agent/pipeline_execute.go
index 81fc06ec..b39e85f1 100644
--- a/pkg/agent/pipeline_execute.go
+++ b/pkg/agent/pipeline_execute.go
@@ -620,6 +620,14 @@ toolLoop:
 			toolResult = tools.ErrorResult("hook returned nil tool result")
 		}
 
+		if !toolResult.IsError && !toolResult.ResponseHandled &&
+			al.publishStructuredResponseIfNeeded(ctx, ts.channel, ts.chatID, ts.sessionKey, toolResult.ForLLM) {
+			toolResult.ResponseHandled = true
+			toolResult.Silent = true
+			toolResult.ForUser = ""
+			toolResult.ForLLM = "Structured vineyard report delivered to the user from the skill payload. Do not paste raw JSON; if you reply, provide only a brief confirmation."
+		}
+
 		if len(toolResult.Media) > 0 && toolResult.ResponseHandled {
 			parts := make([]bus.MediaPart, 0, len(toolResult.Media))
 			for _, ref := range toolResult.Media {
diff --git a/pkg/commands/cmd_start.go b/pkg/commands/cmd_start.go
index 8b500aa1..7980d4bf 100644
--- a/pkg/commands/cmd_start.go
+++ b/pkg/commands/cmd_start.go
@@ -8,7 +8,7 @@ func startCommand() Definition {
 		Description: "Start the bot",
 		Usage:       "/start",
 		Handler: func(_ context.Context, req Request, _ *Runtime) error {
-			return req.Reply("Hello! I am PicoClaw 🦞")
+			return req.Reply("PicoClaw 🍇 is the board orchestrator. I load local contracts and skills, and route hardware through nano-os-agent.")
 		},
 	}
 }
diff --git a/pkg/env.go b/pkg/env.go
index b9a77dab..f116f414 100644
--- a/pkg/env.go
+++ b/pkg/env.go
@@ -3,7 +3,7 @@
 package pkg
 
 const (
-	Logo = "🦞"
+	Logo = "🍇"
 	// AppName is the name of the app
 	AppName = "PicoClaw"
 
diff --git a/pkg/gateway/gateway.go b/pkg/gateway/gateway.go
index c95f586d..68b8898c 100644
--- a/pkg/gateway/gateway.go
+++ b/pkg/gateway/gateway.go
@@ -487,6 +487,8 @@ func setupAndStartServices(
 		fmt.Println("✓ Device event service started")
 	}
 
+	runningServices.HealthServer.SetReady(true)
+
 	return runningServices, nil
 }
 
@@ -494,6 +496,10 @@ func stopAndCleanupServices(runningServices *services, shutdownTimeout time.Dura
 	shutdownCtx, shutdownCancel := context.WithTimeout(context.Background(), shutdownTimeout)
 	defer shutdownCancel()
 
+	if runningServices.HealthServer != nil {
+		runningServices.HealthServer.SetReady(false)
+	}
+
 	// reload should not stop channel manager
 	if !isReload && runningServices.ChannelManager != nil {
 		runningServices.ChannelManager.StopAll(shutdownCtx)
