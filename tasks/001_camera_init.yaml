- hypothesis_ref: h002
  id: cam_001
  name: Camera Sensor Init + Capture
  priority: 2
  status: pending
  steps:
    - action: capture_image
      expect:
        status: success
      id: capture_frame
      max_retries: 2
      parameters:
        output_path: /tmp/test_capture.jpg
      timeout: 30
  success_criteria:
    - test -f /tmp/test_capture.jpg
